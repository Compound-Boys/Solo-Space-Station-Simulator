#!/bin/bash
# AppRun — entry point executed when the AppImage is launched.
# Resolves the mount directory and runs the bundled PyInstaller binary.
HERE="$(dirname "$(readlink -f "${0}")")"
export PATH="${HERE}/usr/bin:${PATH}"
exec "${HERE}/usr/bin/SpaceStationExplorer" "$@"
